#include <stdio.h>

int main()
{
  int n = 0;
  float sum = 0;
  
  do {
    n++;
    sum = sum + ((float)1 / (float)n);  
  }
  while (sum < 5);

  printf("Value is %d\r\n", n);
    printf("sum = %f", sum);


  return 0;
}
